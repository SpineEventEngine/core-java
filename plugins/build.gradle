description = ''
apply plugin: 'maven'
apply plugin: 'groovy'
apply plugin: 'idea'

repositories {
    mavenLocal()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
}

sourceSets {
    main {
        resources.srcDirs += files("plugin/src/main/resources")
        groovy.srcDirs = ["plugin/src/main/groovy"]
    }
}