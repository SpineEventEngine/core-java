description = ''
dependencies {
    compile group: 'com.google.protobuf', name: 'protobuf-java', version: "${project.PROTOBUF_VERSION}"
    compile group: 'io.grpc', name: 'grpc-all', version: '0.7.1'
    compile project(':core' +
            '-java')
    compile project(':grpc')
}
